
╔══════════════════════════════════════════════════════════════╗
║          TURBO REFER V2 — TBC BOT (TPY CODE)                ║
║   Copy each command's code into TBC platform separately     ║
╚══════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔧 BOT SETTINGS (Bot.saveData তে save করুন)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL  = "https://YOUR-APP.onrender.com"   ← Render URL
API_SECRET   = "your_secret_key_here_change_this" ← Same as render.yaml
OWNER_ID     = 123456789                           ← আপনার Telegram ID

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: /start
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNER_ID = Bot.getData("owner_id")
whitelist = Bot.getData("whitelist") or []

if str(u) != str(OWNER_ID) and str(u) not in [str(x) for x in whitelist]:
    bot.sendMessage(
        chat_id=u,
        text="🚫 <b>Access Denied!</b>\n\nআপনার কাছে এই bot ব্যবহারের অনুমতি নেই।\nOwner এর সাথে যোগাযোগ করুন।",
        parse_mode="HTML"
    )
    Bot.stop()

bot.sendMessage(
    chat_id=u,
    text=(
        "⚡ <b>𝗧𝗨𝗥𝗕𝗢 𝗥𝗘𝗙𝗘𝗥 𝗩𝟮</b> ⚡\n"
        "━━━━━━━━━━━━━━━━━━━━━\n"
        "🤖 <i>Telegram Auto-Refer System</i>\n"
        "📌 Version: <code>2.1.2</code>\n"
        "━━━━━━━━━━━━━━━━━━━━━\n\n"
        "📋 <b>Main Menu:</b>"
    ),
    parse_mode="HTML",
    reply_markup={
        "inline_keyboard": [
            [{"text": "👥 Accounts", "callback_data": "menu_accounts"},
             {"text": "🚀 Referral", "callback_data": "menu_refer"}],
            [{"text": "📢 Join/Leave", "callback_data": "menu_channels"},
             {"text": "💬 Send Msg", "callback_data": "menu_message"}],
            [{"text": "🛡️ Ban System", "callback_data": "menu_ban"},
             {"text": "👑 Whitelist", "callback_data": "menu_whitelist"}],
            [{"text": "ℹ️ Status", "callback_data": "menu_status"}]
        ]
    }
)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: callback_query (inline button handler)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNER_ID = Bot.getData("owner_id")
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")
whitelist = Bot.getData("whitelist") or []
data = message.callback_query.data

# Auth check
if str(u) != str(OWNER_ID) and str(u) not in [str(x) for x in whitelist]:
    bot.answerCallbackQuery(callback_query_id=message.callback_query.id, text="🚫 Access Denied!")
    Bot.stop()

bot.answerCallbackQuery(callback_query_id=message.callback_query.id, text="⏳ Loading...")

# ── ACCOUNTS MENU ──
if data == "menu_accounts":
    bot.editMessageText(
        chat_id=u,
        message_id=message.message_id,
        text="👥 <b>Account Manager</b>",
        parse_mode="HTML",
        reply_markup={
            "inline_keyboard": [
                [{"text": "➕ Add Account", "callback_data": "acc_add"}],
                [{"text": "📋 View Accounts", "callback_data": "acc_view"}],
                [{"text": "🗑️ Delete Account", "callback_data": "acc_delete"}],
                [{"text": "🔙 Back", "callback_data": "back_main"}]
            ]
        }
    )

# ── REFERRAL MENU ──
elif data == "menu_refer":
    bot.editMessageText(
        chat_id=u,
        message_id=message.message_id,
        text="🚀 <b>Referral Panel</b>\n\nMethod বেছে নিন:",
        parse_mode="HTML",
        reply_markup={
            "inline_keyboard": [
                [{"text": "✅ No Captcha", "callback_data": "refer_no_captcha"}],
                [{"text": "😀 Emoji Captcha", "callback_data": "refer_emoji"}],
                [{"text": "🔢 Math Captcha", "callback_data": "refer_math"}],
                [{"text": "🖱️ Button Captcha", "callback_data": "refer_button"}],
                [{"text": "🔙 Back", "callback_data": "back_main"}]
            ]
        }
    )

# ── CHANNEL MENU ──
elif data == "menu_channels":
    bot.editMessageText(
        chat_id=u,
        message_id=message.message_id,
        text="📢 <b>Join / Leave Channels</b>",
        parse_mode="HTML",
        reply_markup={
            "inline_keyboard": [
                [{"text": "➕ Join Channels", "callback_data": "ch_join"}],
                [{"text": "➖ Leave Channels", "callback_data": "ch_leave"}],
                [{"text": "🔙 Back", "callback_data": "back_main"}]
            ]
        }
    )

# ── MESSAGE MENU ──
elif data == "menu_message":
    bot.editMessageText(
        chat_id=u,
        message_id=message.message_id,
        text="💬 <b>Send Message</b>\n\nসব account থেকে message পাঠান",
        parse_mode="HTML",
        reply_markup={
            "inline_keyboard": [
                [{"text": "📤 Send to User/Bot", "callback_data": "msg_send"}],
                [{"text": "🔙 Back", "callback_data": "back_main"}]
            ]
        }
    )

# ── BAN MENU ──
elif data == "menu_ban":
    bot.editMessageText(
        chat_id=u,
        message_id=message.message_id,
        text="🛡️ <b>Ban System</b>",
        parse_mode="HTML",
        reply_markup={
            "inline_keyboard": [
                [{"text": "🔨 Ban User", "callback_data": "ban_user"}],
                [{"text": "✅ Unban User", "callback_data": "unban_user"}],
                [{"text": "📋 Ban List", "callback_data": "ban_list"}],
                [{"text": "🔙 Back", "callback_data": "back_main"}]
            ]
        }
    )

# ── WHITELIST MENU ──
elif data == "menu_whitelist":
    bot.editMessageText(
        chat_id=u,
        message_id=message.message_id,
        text="👑 <b>Whitelist Manager</b>\n\nApproved users এখানে manage করুন",
        parse_mode="HTML",
        reply_markup={
            "inline_keyboard": [
                [{"text": "➕ Add User", "callback_data": "wl_add"}],
                [{"text": "➖ Remove User", "callback_data": "wl_remove"}],
                [{"text": "📋 View List", "callback_data": "wl_view"}],
                [{"text": "🔙 Back", "callback_data": "back_main"}]
            ]
        }
    )

# ── STATUS ──
elif data == "menu_status":
    res = HTTP.get(BACKEND_URL + "/ping", headers={"x-api-secret": API_SECRET})
    if res and res.get("ping") == "pong":
        status = "🟢 Online"
    else:
        status = "🔴 Offline"
    accs_res = HTTP.get(BACKEND_URL + "/accounts", headers={"x-api-secret": API_SECRET})
    acc_count = accs_res.get("count", 0) if accs_res else "?"
    bot.editMessageText(
        chat_id=u,
        message_id=message.message_id,
        text=(
            f"ℹ️ <b>System Status</b>\n\n"
            f"🖥️ Backend: {status}\n"
            f"👥 Accounts: {acc_count}\n"
            f"⚡ Version: 2.1.2"
        ),
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Back", "callback_data": "back_main"}]]}
    )

# ── BACK TO MAIN ──
elif data == "back_main":
    Bot.runCommand("start")

# ── VIEW ACCOUNTS ──
elif data == "acc_view":
    res = HTTP.get(BACKEND_URL + "/accounts", headers={"x-api-secret": API_SECRET})
    if not res or res.get("count", 0) == 0:
        text = "📭 কোনো account নেই! ➕ Add করুন।"
    else:
        lines = [f"👥 <b>Saved Accounts ({res['count']})</b>\n"]
        for i, acc in enumerate(res["accounts"], 1):
            lines.append(f"{i}. <code>{acc['session_name']}</code> (ID: {acc['api_id']})")
        text = "\n".join(lines)
    bot.editMessageText(
        chat_id=u, message_id=message.message_id, text=text, parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Back", "callback_data": "menu_accounts"}]]}
    )

# ── REFER METHODS ──
elif data in ("refer_no_captcha", "refer_emoji", "refer_math", "refer_button"):
    method_map = {
        "refer_no_captcha": "no_captcha",
        "refer_emoji": "emoji",
        "refer_math": "math",
        "refer_button": "button"
    }
    User.saveData("refer_method", method_map[data])
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text=f"🔗 <b>Referral Link পাঠান</b>\n\nBot referral link টি পাঠান:\n<i>https://t.me/botname?start=xxxxx</i>",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_refer")

# ── JOIN/LEAVE ──
elif data == "ch_join":
    User.saveData("ch_action", "join")
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="📢 <b>Join Channels</b>\n\nChannel link/username গুলো পাঠান (comma দিয়ে আলাদা করুন):\n\n<i>@channel1, @channel2, https://t.me/channel3</i>",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_channels")

elif data == "ch_leave":
    User.saveData("ch_action", "leave")
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="📢 <b>Leave Channels</b>\n\nChannel link/username গুলো পাঠান (comma দিয়ে আলাদা করুন):\n\n<i>@channel1, @channel2</i>",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_channels")

# ── SEND MESSAGE ──
elif data == "msg_send":
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="💬 <b>Target পাঠান</b>\n\nUsername বা user ID পাঠান:\n<i>@username অথবা 123456789</i>",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("msg_get_target")

# ── BAN ──
elif data == "ban_user":
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="🔨 <b>Ban User</b>\n\nBan করতে User ID পাঠান:",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_ban")

elif data == "unban_user":
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="✅ <b>Unban User</b>\n\nUnban করতে User ID পাঠান:",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_unban")

elif data == "ban_list":
    ban_list = Bot.getData("ban_list") or []
    if not ban_list:
        text = "📭 Ban list খালি!"
    else:
        text = "🛡️ <b>Banned Users:</b>\n\n" + "\n".join([f"• <code>{uid}</code>" for uid in ban_list])
    bot.editMessageText(
        chat_id=u, message_id=message.message_id, text=text, parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Back", "callback_data": "menu_ban"}]]}
    )

# ── WHITELIST ──
elif data == "wl_add":
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="👑 <b>Add to Whitelist</b>\n\nUser ID পাঠান:",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_wl_add")

elif data == "wl_remove":
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="➖ <b>Remove from Whitelist</b>\n\nUser ID পাঠান:",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_wl_remove")

elif data == "wl_view":
    wl = Bot.getData("whitelist") or []
    if not wl:
        text = "📭 Whitelist খালি!"
    else:
        text = "👑 <b>Whitelisted Users:</b>\n\n" + "\n".join([f"• <code>{uid}</code>" for uid in wl])
    bot.editMessageText(
        chat_id=u, message_id=message.message_id, text=text, parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Back", "callback_data": "menu_whitelist"}]]}
    )

# ── ADD ACCOUNT (Step 1: session name) ──
elif data == "acc_add":
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="➕ <b>Add Account</b>\n\nStep 1/4: একটি নাম দিন account-এর জন্য:\n<i>উদাহরণ: acc1, myphone</i>",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("acc_get_name")

# ── DELETE ACCOUNT ──
elif data == "acc_delete":
    bot.editMessageText(
        chat_id=u, message_id=message.message_id,
        text="🗑️ <b>Delete Account</b>\n\nSession name পাঠান:",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "❌ Cancel", "callback_data": "back_main"}]]}
    )
    Bot.handleNextCommand("do_acc_delete")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: acc_get_name (Account Add Step 1)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
User.saveData("acc_session_name", msg)
bot.sendMessage(
    chat_id=u,
    text=f"✅ Name: <code>{msg}</code>\n\nStep 2/4: API ID পাঠান:\n<i>my.telegram.org থেকে পাবেন</i>",
    parse_mode="HTML"
)
Bot.handleNextCommand("acc_get_api_id")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: acc_get_api_id (Account Add Step 2)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
if not isNumeric(msg):
    bot.sendMessage(chat_id=u, text="❌ API ID must be a number! আবার পাঠান:")
    Bot.handleNextCommand("acc_get_api_id")
    Bot.stop()
User.saveData("acc_api_id", int(msg))
bot.sendMessage(
    chat_id=u,
    text=f"✅ API ID: <code>{msg}</code>\n\nStep 3/4: API Hash পাঠান:",
    parse_mode="HTML"
)
Bot.handleNextCommand("acc_get_api_hash")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: acc_get_api_hash (Account Add Step 3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
User.saveData("acc_api_hash", msg)
bot.sendMessage(
    chat_id=u,
    text=f"✅ API Hash: <code>{msg[:8]}...</code>\n\nStep 4/4: Phone number পাঠান:\n<i>উদাহরণ: +8801XXXXXXXXX</i>",
    parse_mode="HTML"
)
Bot.handleNextCommand("acc_get_phone")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: acc_get_phone (Account Add Step 4 - Send OTP)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")

phone = msg
session_name = User.getData("acc_session_name")
api_id = User.getData("acc_api_id")
api_hash = User.getData("acc_api_hash")

User.saveData("acc_phone", phone)

bot.sendMessage(chat_id=u, text="⏳ OTP পাঠানো হচ্ছে...", parse_mode="HTML")

res = HTTP.post(
    BACKEND_URL + "/accounts/request-code",
    json={
        "session_name": session_name,
        "api_id": api_id,
        "api_hash": api_hash,
        "phone": phone
    },
    headers={"x-api-secret": API_SECRET}
)

if res and res.get("status") == "code_sent":
    User.saveData("acc_phone_code_hash", res["phone_code_hash"])
    bot.sendMessage(
        chat_id=u,
        text=f"📱 <b>OTP Sent!</b>\n\nআপনার phone-এ OTP এসেছে। Code টি পাঠান:\n<i>digits only, e.g: 12345</i>",
        parse_mode="HTML"
    )
    Bot.handleNextCommand("acc_verify_otp")
else:
    err = res.get("message", "Unknown error") if res else "Backend offline"
    bot.sendMessage(chat_id=u, text=f"❌ Error: {err}", parse_mode="HTML")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: acc_verify_otp (Verify login code)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")

session_name = User.getData("acc_session_name")
api_id = User.getData("acc_api_id")
api_hash = User.getData("acc_api_hash")
phone = User.getData("acc_phone")
phone_code_hash = User.getData("acc_phone_code_hash")

bot.sendMessage(chat_id=u, text="⏳ Verifying...", parse_mode="HTML")

res = HTTP.post(
    BACKEND_URL + "/accounts/verify-code",
    json={
        "session_name": session_name,
        "api_id": api_id,
        "api_hash": api_hash,
        "phone": phone,
        "code": msg,
        "phone_code_hash": phone_code_hash
    },
    headers={"x-api-secret": API_SECRET}
)

if res and res.get("status") == "success":
    bot.sendMessage(
        chat_id=u,
        text=f"🎉 <b>Account Added!</b>\n\n{res['message']}",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
    )
elif res and res.get("status") == "2fa_needed":
    bot.sendMessage(chat_id=u, text="🔐 2FA Password পাঠান:", parse_mode="HTML")
    Bot.handleNextCommand("acc_verify_2fa")
else:
    err = res.get("message", "Unknown") if res else "Backend offline"
    bot.sendMessage(chat_id=u, text=f"❌ Error: {err}", parse_mode="HTML")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: acc_verify_2fa
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")
session_name = User.getData("acc_session_name")

res = HTTP.post(
    BACKEND_URL + "/accounts/verify-2fa",
    params={"session_name": session_name, "password": msg},
    headers={"x-api-secret": API_SECRET}
)

if res and res.get("status") == "success":
    bot.sendMessage(
        chat_id=u,
        text=f"🎉 <b>2FA Verified!</b>\n{res['message']}",
        parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
    )
else:
    err = res.get("message", "Unknown") if res else "Error"
    bot.sendMessage(chat_id=u, text=f"❌ {err}", parse_mode="HTML")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_acc_delete
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")

res = HTTP.delete(
    BACKEND_URL + "/accounts/" + msg,
    headers={"x-api-secret": API_SECRET}
)
if res and res.get("status") == "success":
    bot.sendMessage(chat_id=u, text=f"✅ <b>Deleted:</b> <code>{msg}</code>", parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]})
else:
    bot.sendMessage(chat_id=u, text=f"❌ Error deleting account", parse_mode="HTML")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_refer
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")
method = User.getData("refer_method")

bot.sendMessage(chat_id=u, text=f"⏳ <b>Referral চালু হচ্ছে...</b>\nMethod: <code>{method}</code>", parse_mode="HTML")

res = HTTP.post(
    BACKEND_URL + "/refer",
    json={"bot_link": msg, "method": method},
    headers={"x-api-secret": API_SECRET},
    timeout=120
)

if res and res.get("status") == "done":
    r = res
    lines = [f"🎉 <b>Referral Done!</b>\n",
             f"✅ Success: {r['success']}",
             f"❌ Failed: {r['failed']}",
             f"📊 Total: {r['total']}\n",
             "<b>Details:</b>"]
    for acc_r in r.get("results", []):
        icon = "✅" if acc_r["status"] == "success" else "❌"
        lines.append(f"{icon} <code>{acc_r['account']}</code>: {acc_r.get('msg','')}")
    bot.sendMessage(
        chat_id=u, text="\n".join(lines), parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
    )
else:
    bot.sendMessage(chat_id=u, text="❌ Backend error বা timeout!", parse_mode="HTML")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_channels
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")
action = User.getData("ch_action")
channels = [c.strip() for c in msg.split(",")]

bot.sendMessage(chat_id=u, text=f"⏳ {action.capitalize()} করা হচ্ছে {len(channels)} টি channel...", parse_mode="HTML")

res = HTTP.post(
    BACKEND_URL + "/channels",
    json={"channels": channels, "action": action},
    headers={"x-api-secret": API_SECRET},
    timeout=120
)

if res and res.get("status") == "done":
    lines = [f"📢 <b>Channel {action.capitalize()} Done!</b>\n"]
    for acc_r in res.get("results", []):
        lines.append(f"👤 <code>{acc_r['account']}</code>:")
        for ch_r in acc_r.get("channels", []):
            lines.append(f"  • {ch_r['channel']}: {ch_r['status']}")
    bot.sendMessage(
        chat_id=u, text="\n".join(lines), parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
    )
else:
    bot.sendMessage(chat_id=u, text="❌ Error!", parse_mode="HTML")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: msg_get_target
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
User.saveData("msg_target", msg)
bot.sendMessage(chat_id=u, text=f"✅ Target: <code>{msg}</code>\n\nএখন message টি পাঠান:", parse_mode="HTML")
Bot.handleNextCommand("do_send_message")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_send_message
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND_URL = Bot.getData("backend_url")
API_SECRET = Bot.getData("api_secret")
target = User.getData("msg_target")

res = HTTP.post(
    BACKEND_URL + "/message",
    json={"target": target, "message": msg},
    headers={"x-api-secret": API_SECRET},
    timeout=60
)

if res and res.get("status") == "done":
    lines = [f"💬 <b>Message Sent!</b>\n"]
    for r in res.get("results", []):
        lines.append(f"• <code>{r['account']}</code>: {r['status']}")
    bot.sendMessage(
        chat_id=u, text="\n".join(lines), parse_mode="HTML",
        reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
    )
else:
    bot.sendMessage(chat_id=u, text="❌ Error sending message!", parse_mode="HTML")

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_ban
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNER_ID = Bot.getData("owner_id")
if str(u) != str(OWNER_ID):
    bot.sendMessage(chat_id=u, text="🚫 Only Owner can ban!")
    Bot.stop()
ban_list = Bot.getData("ban_list") or []
if msg not in [str(x) for x in ban_list]:
    ban_list.append(msg)
    Bot.saveData("ban_list", ban_list)
bot.sendMessage(
    chat_id=u, text=f"🔨 <b>Banned:</b> <code>{msg}</code>", parse_mode="HTML",
    reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_unban
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNER_ID = Bot.getData("owner_id")
if str(u) != str(OWNER_ID):
    bot.sendMessage(chat_id=u, text="🚫 Only Owner can unban!")
    Bot.stop()
ban_list = Bot.getData("ban_list") or []
ban_list = [x for x in ban_list if str(x) != str(msg)]
Bot.saveData("ban_list", ban_list)
bot.sendMessage(
    chat_id=u, text=f"✅ <b>Unbanned:</b> <code>{msg}</code>", parse_mode="HTML",
    reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_wl_add
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNER_ID = Bot.getData("owner_id")
if str(u) != str(OWNER_ID):
    bot.sendMessage(chat_id=u, text="🚫 Only Owner!")
    Bot.stop()
wl = Bot.getData("whitelist") or []
if msg not in [str(x) for x in wl]:
    wl.append(msg)
    Bot.saveData("whitelist", wl)
bot.sendMessage(
    chat_id=u, text=f"👑 <b>Added to Whitelist:</b> <code>{msg}</code>", parse_mode="HTML",
    reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 COMMAND: do_wl_remove
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OWNER_ID = Bot.getData("owner_id")
if str(u) != str(OWNER_ID):
    bot.sendMessage(chat_id=u, text="🚫 Only Owner!")
    Bot.stop()
wl = Bot.getData("whitelist") or []
wl = [x for x in wl if str(x) != str(msg)]
Bot.saveData("whitelist", wl)
bot.sendMessage(
    chat_id=u, text=f"➖ <b>Removed from Whitelist:</b> <code>{msg}</code>", parse_mode="HTML",
    reply_markup={"inline_keyboard": [[{"text": "🔙 Main Menu", "callback_data": "back_main"}]]}
)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚙️ BOT SETUP: TBC তে Bot.saveData() দিয়ে save করুন
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
/setup command এ এই code রাখুন (once run করুন):

Bot.saveData("backend_url", "https://YOUR-APP.onrender.com")
Bot.saveData("api_secret", "your_secret_key_here_change_this")
Bot.saveData("owner_id", 123456789)   # আপনার Telegram ID
Bot.saveData("whitelist", [])
Bot.saveData("ban_list", [])
bot.sendMessage(chat_id=u, text="✅ Setup complete!")
